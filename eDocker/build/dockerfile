# FROM ubuntu:latest        
FROM ubuntu      
RUN apt-get update -y
RUN groupadd -r gDocker && useradd -r -g gDocker uDocker
USER uDocker
RUN ["pwd"]
WORKDIR /home/uDocker
COPY ./build/eDocker ./              
RUN ["ls","-l","./eDocker"]         
CMD ["./eDocker"]