//
syntax = "proto3";

package api;

service api{
    rpc Ping(Request)returns(Reply){};
}

message Request {
    string Message = 1;
}

message Reply {
    string Message = 1;
}

// protoc --go_out=plugins=grpc:. *.proto