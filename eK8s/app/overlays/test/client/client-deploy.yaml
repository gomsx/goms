apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-deploy
  labels:
    app: client # istio 规范
    version: v1 # istio 规范
spec:
  replicas: 1
  selector:
    matchLabels:
      app: client  #选择 pod label
      version: v1
  template:
    metadata:
      labels:
        app: client #标注 pod label
        version: v1
    spec:
      containers:
      - name: client
        image: dockerxpub/goms-clienttest:v1.1.4
        imagePullPolicy: IfNotPresent
        command: ["sh", "/bash/test.sh","100000","v1","user-svc"] 

