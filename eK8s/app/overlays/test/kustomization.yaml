apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: test-
commonLabels:
    variant: test
commonAnnotations:
    note: Hello, I am test!
bases:
- ../../base

resources:
- client/client-deploy.yaml
- client/client-svc.yaml

configMapGenerator:
- name: cm-user
  files:
  - configs/app.yaml
  - configs/grpc.yaml
  - configs/http.yaml
  - configs/mysql.yaml
  - configs/redis.yaml

patchesStrategicMerge:
  - deploy-path/user-deploy.yaml

