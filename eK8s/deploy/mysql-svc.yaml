apiVersion: v1
kind: Service
metadata:
  name: mysql-svc
  namespace: goms
  labels:
    svc: mysql-svc
spec:
  type: NodePort
  ports:
  - port: 3306
    protocol: TCP
    targetPort: 3306
    name: http
    nodePort: 31001
  selector:
    app: mysql #选择 pod label