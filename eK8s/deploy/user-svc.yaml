apiVersion: v1
kind: Service
metadata:
  name: user-svc
  namespace: goms
  labels:
    svc: user-svc
spec:
  type: NodePort
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
    name: http
    nodePort: 31003
  selector:
    app: user #选择 pod label