apiVersion: v1
kind: Service
metadata:
  name: clienttest-svc
  labels:
    svc: clienttest-svc
spec:
  type: NodePort
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
    nodePort: 32024
  selector:
    app: clienttest #选择 pod label

